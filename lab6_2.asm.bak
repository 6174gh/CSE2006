; packed BCD to hex
ASSUME CS:CODE
CODE SEGMENT
	START:
	MOV BL, 25H
	AND BL, 0FH
	MOV BX, AX
	MOV AL, 25H
	AND AL, F0H
	MOV CL, 04H
	ROR AL, CL
	MOV CX, 0AH
	MUL CX
	ADD AX, BX
	HLT
CODE ENDS
	END START